<% title "Checker" %>

<% form_tag :checkers, :method => :post  do %>
	<%= text_area_tag(:text, params[:text], :size => "80x40") %>
	<br/>
  <%= submit_tag "Check Syntax" %>
  <% begin %>
    <div>
			<%= render :partial => 'testrunner' %>
		</div>
  <% rescue %>
		<p>Hay errores en la generación de la plantilla xml</p>
  <% end %>
<% end -%>