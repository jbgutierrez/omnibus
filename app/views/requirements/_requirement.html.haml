- semantic_form_for @requirement do |f|
  = f.error_messages
  = f.inputs :code, :name, :description, :release_version
  - f.inputs do
    = f.input :status, :as => :select, :collection => [ @requirement.status ] + @requirement.state_transitions_names
    = f.input :date
  = f.submit
  ó
  = link_to "Cancelar", requirements_path, :class => 'warn'