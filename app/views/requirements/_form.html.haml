- semantic_form_for @requirement do |f|
  = f.error_messages
  - f.inputs do
    = f.input :name
    = f.input :description
    = f.input :state, :as => :select, :collection => [ @requirement.state ] + @requirement.state_transitions_names
  = f.submit